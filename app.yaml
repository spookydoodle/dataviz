runtime: nodejs10
includes:
  - env_vars.yaml

manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10
# steps:
#   # running docker-compose
#   - name: 'docker/compose:1.26.2'
#     args: ['up', '-d']